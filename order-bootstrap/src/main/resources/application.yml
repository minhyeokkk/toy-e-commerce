spring:
  application:
    name: order
  config:
    import:
      - classpath:order-domain.yml
      - classpath:order-infra-rdb.yml
      - classpath:order-application.yml
      - classpath:product-client.yml
server:
  port: 8081
